#include <stdio.h>
#include <string.h>

int main(void)

{
    int a,b,c;

    printf("3개의 단어를 입력하시오 : ");
    scanf(%s %s %s,a,b,c);

    printf("정렬 :");
    if (strcmp(a,b)>0)
    printf("%s\n",a)
    if (strcmp(a))

}